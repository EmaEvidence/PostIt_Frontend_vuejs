version: 2
jobs:
  build:
    docker:
      # - image: debian:stretch
    steps:
      - checkout
      - run:
          name: build
          command: yarn build
      - run:
          name: test
          command: yarn unit
